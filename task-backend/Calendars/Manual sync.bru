meta {
  name: Manual sync
  type: http
  seq: 4
}

post {
  url: {{base_url}}/calendars/3/sync
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

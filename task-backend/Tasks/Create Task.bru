meta {
  name: Create Task
  type: http
  seq: 2
}

post {
  url: {{base_url}}/tasks
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Cuadernillo de deber del programador",
    "priority": "alta",
    "subject": "IG_Corina"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

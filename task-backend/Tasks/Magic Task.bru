meta {
  name: Magic Task
  type: http
  seq: 3
}

post {
  url: {{base_url}}/magic
  body: json
  auth: inherit
}

body:json {
  {
    "text": "Hacer cuadro comparativo entre android e IOS"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

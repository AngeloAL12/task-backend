meta {
  name: Update Task
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/tasks/40
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "subject": "Ingeniería de Software",
    "priority": "alta"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

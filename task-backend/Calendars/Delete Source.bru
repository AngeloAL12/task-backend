meta {
  name: Delete Source
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/calendars/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

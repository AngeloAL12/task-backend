meta {
  name: List Sources
  type: http
  seq: 2
}

get {
  url: {{base_url}}/calendars
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

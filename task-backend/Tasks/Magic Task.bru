meta {
  name: Magic Task
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/tasks/magic
  body: json
  auth: inherit
}

body:json {
  {
    "text": "Hacer cuadro comparativo entre linux, windows y macos"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

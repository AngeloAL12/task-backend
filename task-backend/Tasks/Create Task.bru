meta {
  name: Create Task
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/tasks
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Probar Bruno",
    "priority": "alta",
    "subject": "DevOps"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
